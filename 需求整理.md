# 包裝記點記錄 - 需求整理

## 資料來源
1. `hr_miss_record_types` -> Supabase 上的失誤類型定義（扣點規章基準）
2. `hr_miss_records` -> Supabase 上的失誤紀錄（實際被記點的明細）

---

## 目標
做一個以**組員自我查詢**為主的唯讀展示網頁，讓組員用手機可隨時查看：
1. 自己在特定時間區間內的「品質考核分數與記點明細」。
2. 廠內規定的「記點規章手冊」，清楚知道哪些行為會被扣點、扣幾分。

---

## 使用對象
- 所有人（組員、機台人員、組長、管理者）免登入、免驗證工號，公開唯讀查詢。

---

## 展示功能規劃（重構版）

### 範圍限制
- 只實作「頁面一：個人品質記點查詢」與「頁面二：記點規章手冊」。
- 資料來源僅限 `hr_miss_record_types`（規範項目）、`hr_miss_records`（記點紀錄）。
- HTML 畫面僅呈現上述兩個頁面內容，以導覽按鈕切換。

### 頁面一：個人品質記點與考核分數查詢
- **個人搜尋與區間篩選**：
  - 組員可輸入或選擇自己的姓名/工號（前端自動篩選下拉選單）。
  - 可選擇查詢的日期區間（預設為最近三個月，限制最長三個月內）。
  - 提供「考核身份」手動切換開關：「一般人員 (1200 分)」與「機台人員 (1800 分)」。系統會根據被查詢組員最近的記點職務自動預設（若職務包含“機台”則預設為機台人員，否則為一般人員）。
- **品質考核分數看板 (Quality Score Dashboard)**：
  - 大字顯示當前品質考核總得分（一般人員滿分 1200 分，機台人員滿分 1800 分）。
  - 分別顯示三大品質維度的得分進度條與數值：
    1. **生產作業與機台操作**（一般人員滿分 600 分 / 機台人員滿分 1200 分）
    2. **現場紀律與環境維護**（滿分 200 分）
    3. **品質異常與首件確認**（滿分 400 分）
  - 當點數有扣除時，進度條顏色會呈現對應的警示變化，直觀看到扣分情況。
- **個人記點明細表**：
  - 只顯示該員工在選定區間內的被記點紀錄，包含：日期、考核維度、記點項目（item_code 與細項名稱）、扣分點數、以及組長寫的備註事由。

### 頁面二：記點規章手冊
- **規章總覽**：
  - 將所有資料庫中的 `hr_miss_record_types` 依據上述 **「生產作業、現場紀律、品質異常」** 三大考核維度進行分類展示。
  - 方便組員直接查閱做錯哪些事情會被扣幾點。
- **快速篩選**：
  - 提供職位篩選（全員、組員、機台等），方便組員只看與自己相關的規範項目。

---

## 技術選型
- **後端**：Python + FastAPI
  - 連接 Supabase PostgreSQL（psycopg2）
  - 提供 REST API（唯讀，安全 SELECT）
- **前端**：簡單靜態 HTML + JS
  - 手機優先（RWD 設計）

### 前端架構與結構（已同步 index.html）
- 採單頁式結構（SPA-like，不換頁），以區塊切換呈現個人查詢與規章手冊兩個頁面。
- 前端保留 API 串接邏輯與無 API 時的靜態備用資料，隨時可進行切換與渲染。

### 檔案責任劃分
- `index.html`：前端 UI、互動邏輯、品質分數折算運算與規章分組顯示。
- `需求整理.md`：產品需求、品質三大維度定義、API 規格與實作說明。
- `api_server.py`：後端 API 伺服器，提供員工清單查詢、記點明細篩選。

---

## 品質三大考核維度之資料分類對照表

系統在計算分數與展示規章時，會將資料庫中 `hr_miss_record_types.category`（或 `hr_miss_records.category`）與 `subcategory` 對應至以下三個維度：

| 三大考核維度 | 資料庫分類對照 (`category` / `subcategory`) |
|---|---|
| **生產作業與機台操作** | <ul><li>`作業準備與確認`（不含 `未檢查首包`）</li><li>`生產加工作業`</li><li>`機台袋膜設定`</li><li>`機台保養與維修聯絡`</li><li>`備料與發料作業`</li><li>`庫存管理與盤點`</li><li>`原料控管與盤點`</li><li>`生產執行與進度回報`（不含 `未檢查首包`）</li><li>`生產排程規劃與追蹤`</li></ul> |
| **現場紀律與環境維護** | <ul><li>`收尾與環境維護`</li><li>`效率與團隊作業`</li><li>`安全與稽核規範`</li><li>`現場人員指派與管理`</li><li>`現場支援與協調`</li><li>`行政庶務` / `行政庶務與費用管理`</li><li>`ERP作業與流程`</li></ul> |
| **品質異常與首件確認** | <ul><li>`品質` / `品質異常` / `品質維護與異常處理` / `現場品質控管`</li><li>`履歷作業`</li><li>`標籤印製`</li><li>`系統單據與對帳`</li><li>細項包含 `未檢查首包` 的所有項目（不管原分類）</li></ul> |

---

## API 規格草案

### 1. GET /api/v1/employees
- **用途**：取得所有曾被記錄的組員名單，供前端搜尋框下拉選單使用
- **回傳格式**：
  ```json
  {
    "items": [
      {
        "employee_code": "0688",
        "employee_name": "陳怡靜",
        "latest_position": "1組員"
      }
    ]
  }
  ```

### 2. GET /api/v1/records
- **用途**：查詢實際記點明細列表
- **Query Params**：
  - `start_date`: date，可選（例：2026-06-01）
  - `end_date`: date，可選
  - `employee_code`: string，可選（篩選特定員工，如 `0688`）
  - `limit`: int，可選，預設 100
- **回傳格式**：
  ```json
  {
    "items": [
      {
        "id": 123,
        "event_date": "2026-06-05",
        "employee_id": 99,
        "employee_code": "0688",
        "employee_name": "陳怡靜",
        "employee_position": "1組員",
        "item_code": "0全員-8",
        "category": "效率與團隊作業",
        "subcategory": "因重工導致缺誤裝排程效率",
        "points": -1,
        "notes": "做PLB洋菇..."
      }
    ]
  }
  ```

### 3. GET /api/v1/types
- **用途**：取得所有記點類型清單（規章與手冊分組使用）
- **Query Params**：
  - `include_unused`: bool，預設 true
- **回傳格式**：
  ```json
  {
    "items": [
      {
        "miss_type_id": 1,
        "item_code": "0全員-1",
        "jobs": "0",
        "category": "安全與稽核規範",
        "subcategory": "使用拖板車，未依規定操作",
        "points": -1,
        "usage_count": 0,
        "is_unused": true
      }
    ]
  }
  ```
